\usetikzlibrary{arrows.meta,decorations.pathmorphing}

\begin{tikzpicture}[x=1cm,y=1cm,>=Stealth,line cap=round,line join=round
axis style/.style={gray!60!black, thick},]

  % Parameters
  \def\X{12}
  \def\L{6}
  \def\H{2}
  \def\P{5}

  % Top and bottom guide walls
  \draw[axis style] (0,\H) -- (\L,\H);
  \draw[axis style] (0,0) -- (\L,0);
	
  % Left wall of container
  \draw[thick] (-0.18,0.05) -- (-0.18,\H-0.05);
  \draw[thick] (0.18,0.05) -- (0.18,\H-0.05);
  \node[below] at (0,1.2) {$\Delta$};
  % Gas region outline (light fill optional)
  % \fill[gray!10] (0,0) rectangle (\P,\H);

  % Piston
  \fill[black] (\L-1,\H-0.05) rectangle (\L-0.7,0.05);

  % spring coils
\draw[thick,
        decoration={
            coil,                % This is the key: use the 'coil' shape
            segment length=4mm,  % Distance from loop to loop (compression)
            amplitude=6mm,        % Height/width of the coil loops
            aspect=0.4           % Flattening factor (0 = flat line, 1 = perfect circles)
        }, 
        decorate                 % Applies the chosen decoration
    ] (\P+0.2,1) -- (\P+2.4,1);          % Draws the decorated path from x=1 to x=5

	% external bath particles
    % Particles
  \foreach \x/\y in {
    -0.75/1.75,
    -0.48/1.20,
    -0.74/0.48,
    -0.5/0.2,
  }{
    \fill[gray!15] (\x,\y) circle (0.15);
    \draw[thick] (\x,\y) circle (0.15);
  }

  % Particles
  \foreach \x/\y in {
    0.75/1.75,
    0.48/1.20,
    0.74/0.48,
    2.05/1.08,
    1.95/0.62,
    1.38/1.72,
    2.6/1,
    2.95/0.62,
    2.38/1.72,
    2.68/0.40,
    3.5/0.72,
    3.64/1.22,
    4.38/0.5,
    4.38/1.72,
  }{
    \fill[gray!55] (\x,\y) circle (0.15);
    \draw[thick] (\x,\y) circle (0.15);
  }

  % x-axis with ticks
  \draw[thick] (0,-0.75) -- (\L+1,-0.75);
  \draw[thick] (0,-0.75) -- (0,-1.05);
  \draw[thick] (\P,-0.75) -- (\P,-1.05);

  % Labels
  \node[below] at (0,-1.18) {$0$};
  \node[below] at (\P,-1.18) {$L$};
  \node[right] at (\L+1.2,-0.75) {$x$};

\end{tikzpicture}